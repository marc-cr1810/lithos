#include "ModelMesh.h"

// ModelMesh.cpp is now minimal since it just wraps ModelMeshData
// All implementation is in ModelMeshData.h

namespace Lithos {
// Empty for now - all methods are inline in header
}
